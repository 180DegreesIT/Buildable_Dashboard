# Database
DATABASE_URL="postgresql://user:password@localhost:5432/buildable_dashboard?schema=public"

# Server
PORT=6001
NODE_ENV=development

# Azure AD / M365 SSO (Phase 1 stub - not required for dev)
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=
AZURE_AD_REDIRECT_URI=http://localhost:6001/api/v1/auth/callback

# Xero API (Phase 1 scaffold - not required for dev)
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
XERO_REDIRECT_URI=http://localhost:6001/api/v1/xero/callback

# Dev auth bypass user (only used when NODE_ENV=development)
DEV_USER_EMAIL=admin@buildable.com.au
DEV_USER_NAME=Dev Admin
